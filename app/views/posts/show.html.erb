<% if can? :edit, @post %>
	<h1><%= markdown @post.title %></h1>

	<div class="row">
	  <div class="span8">
	    <p><%= markdown @post.body %></p>
	  </div>
	  <div class="span4">
	    <% if can? :edit, @post %>
	      <%= link_to "Edit", edit_topic_post_path(@topic,@post), class: 'btn btn-large btn-block' %>
	    <% end %>
	  </div>
	</div>
<% else %>
	<h1>Sorry Yuo need to be a member to see this</h1>	
<% end %>	